{
  "name": "spike-pig3d",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spike: porcetto procedurale in three.js. Non fa parte del workspace pnpm.",
  "scripts": {
    "bundle": "esbuild src/main.ts --bundle --format=iife --minify --target=es2020 --outfile=bundle.js",
    "build": "npm run bundle && node build.mjs",
    "shots": "node shot.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "^1.62.1"
  }
}
